import { TestBed, getTestBed } from '@angular/core/testing';
import { HttpClientTestingModule, HttpTestingController } from '@angular/common/http/testing';
import { take, map } from 'rxjs/operators';
import { CostOfSalesAccountBalanceService } from 'app/entities/cost-of-sales-account-balance/cost-of-sales-account-balance.service';
import { ICostOfSalesAccountBalance, CostOfSalesAccountBalance } from 'app/shared/model/cost-of-sales-account-balance.model';

describe('Service Tests', () => {
  describe('CostOfSalesAccountBalance Service', () => {
    let injector: TestBed;
    let service: CostOfSalesAccountBalanceService;
    let httpMock: HttpTestingController;
    let elemDefault: ICostOfSalesAccountBalance;
    let expectedResult: ICostOfSalesAccountBalance | ICostOfSalesAccountBalance[] | boolean | null;
    beforeEach(() => {
      TestBed.configureTestingModule({
        imports: [HttpClientTestingModule]
      });
      expectedResult = null;
      injector = getTestBed();
      service = injector.get(CostOfSalesAccountBalanceService);
      httpMock = injector.get(HttpTestingController);

      elemDefault = new CostOfSalesAccountBalance(0, 0);
    });

    describe('Service methods', () => {
      it('should find an element', () => {
        const returnedFromService = Object.assign({}, elemDefault);
        service
          .find(123)
          .pipe(take(1))
          .subscribe(resp => (expectedResult = resp.body));

        const req = httpMock.expectOne({ method: 'GET' });
        req.flush(returnedFromService);
        expect(expectedResult).toMatchObject(elemDefault);
      });

      it('should create a CostOfSalesAccountBalance', () => {
        const returnedFromService = Object.assign(
          {
            id: 0
          },
          elemDefault
        );
        const expected = Object.assign({}, returnedFromService);
        service
          .create(new CostOfSalesAccountBalance())
          .pipe(take(1))
          .subscribe(resp => (expectedResult = resp.body));
        const req = httpMock.expectOne({ method: 'POST' });
        req.flush(returnedFromService);
        expect(expectedResult).toMatchObject(expected);
      });

      it('should update a CostOfSalesAccountBalance', () => {
        const returnedFromService = Object.assign(
          {
            balance: 1
          },
          elemDefault
        );

        const expected = Object.assign({}, returnedFromService);
        service
          .update(expected)
          .pipe(take(1))
          .subscribe(resp => (expectedResult = resp.body));
        const req = httpMock.expectOne({ method: 'PUT' });
        req.flush(returnedFromService);
        expect(expectedResult).toMatchObject(expected);
      });

      it('should return a list of CostOfSalesAccountBalance', () => {
        const returnedFromService = Object.assign(
          {
            balance: 1
          },
          elemDefault
        );
        const expected = Object.assign({}, returnedFromService);
        service
          .query()
          .pipe(
            take(1),
            map(resp => resp.body)
          )
          .subscribe(body => (expectedResult = body));
        const req = httpMock.expectOne({ method: 'GET' });
        req.flush([returnedFromService]);
        httpMock.verify();
        expect(expectedResult).toContainEqual(expected);
      });

      it('should delete a CostOfSalesAccountBalance', () => {
        service.delete(123).subscribe(resp => (expectedResult = resp.ok));

        const req = httpMock.expectOne({ method: 'DELETE' });
        req.flush({ status: 200 });
        expect(expectedResult);
      });
    });

    afterEach(() => {
      httpMock.verify();
    });
  });
});
